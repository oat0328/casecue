import React from'react';import{useParams}from'react-router-dom';import{WORKSPACES}from'@/lib/workspaces';
export default function WorkspaceComingSoon(){const{workspace}=useParams(),w=WORKSPACES[workspace];return <div className="rounded-3xl bg-slate-950 p-9 text-white"><div className="text-xs font-black uppercase tracking-[.2em] text-sky-300">{w?.name||'CaseCue Workspace'}</div><h1 className="mt-3 text-3xl font-black">The workspace shell is ready.</h1><p className="mt-3 max-w-2xl text-slate-300">{w?.description} We can now build this role’s actual day without changing CaseCue SPED.</p></div>}
