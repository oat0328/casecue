import React from 'react'; import { NavLink, Outlet } from 'react-router-dom'; import { FileEdit, ShieldCheck } from 'lucide-react'; import PageHeader from '@/components/PageHeader'; import { cn } from '@/lib/utils';
const tabs=[['IEP Studio','/ieps/studio',FileEdit],['CaseCue Check','/ieps/check',ShieldCheck]];
export default function IEPWorkspace(){return <div><PageHeader title="IEPs" subtitle="Build the IEP, then review it with CaseCue Check in the same workspace." icon={FileEdit}/><div className="mb-6 flex gap-2 rounded-2xl border bg-white p-2">{tabs.map(([label,path,Icon])=><NavLink key={path} to={path} className={({isActive})=>cn('flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold',isActive?'bg-slate-950 text-white':'text-slate-600 hover:bg-slate-100')}><Icon className="h-4 w-4"/>{label}</NavLink>)}</div><Outlet/></div>}