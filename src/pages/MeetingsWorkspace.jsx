import React from 'react'; import { NavLink, Outlet } from 'react-router-dom'; import { UsersRound, ClipboardList } from 'lucide-react'; import PageHeader from '@/components/PageHeader'; import { cn } from '@/lib/utils';
const tabs=[['Meeting Center','/meetings-workspace/center',UsersRound],['Meeting Navigator','/meetings-workspace/navigator',ClipboardList]];
export default function MeetingsWorkspace(){return <div><PageHeader title="Meetings" subtitle="Manage upcoming meetings and open the step-by-step Meeting Navigator from one place." icon={UsersRound}/><div className="mb-6 flex gap-2 rounded-2xl border bg-white p-2">{tabs.map(([label,path,Icon])=><NavLink key={path} to={path} className={({isActive})=>cn('flex items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold',isActive?'bg-slate-950 text-white':'text-slate-600 hover:bg-slate-100')}><Icon className="h-4 w-4"/>{label}</NavLink>)}</div><Outlet/></div>}