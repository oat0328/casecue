import React from 'react';
import { NavLink, Outlet } from 'react-router-dom';
import { BarChart3, Timer, ClipboardCheck, Network, Archive } from 'lucide-react';
import PageHeader from '@/components/PageHeader'; import { cn } from '@/lib/utils';
const tabs=[['Student Progress','/progress/data',BarChart3],['Record a Session','/progress/sessions',Timer],['Friday Progress Monitoring','/progress/monitoring',ClipboardCheck],['Instructional Goal Groups','/progress/groups',Network],['Work Samples & Evidence','/progress/evidence',Archive]];
export default function ProgressWorkspace(){return <div><PageHeader title="Goal Progress" subtitle="Record sessions, monitor IEP goals, see who needs data, and keep student evidence connected to progress." icon={BarChart3}/><div className="mb-6 flex gap-2 overflow-x-auto rounded-2xl border bg-white p-2">{tabs.map(([label,path,Icon])=><NavLink key={path} to={path} className={({isActive})=>cn('flex shrink-0 items-center gap-2 rounded-xl px-4 py-2.5 text-sm font-bold',isActive?'bg-slate-950 text-white':'text-slate-600 hover:bg-slate-100')}><Icon className="h-4 w-4"/>{label}</NavLink>)}</div><Outlet/></div>}