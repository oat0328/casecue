import React,{useState}from'react';import{ShieldCheck,CheckCircle2}from'lucide-react';
export default function SubstituteHome(){const[type,setType]=useState('general'),[done,setDone]=useState([]);const flow=type==='sped'?['Review teacher-approved student supports','Follow the class/resource schedule','Use only accommodations shared for today','Capture objective notes','Leave a clean handoff for the teacher']:['Review today’s teacher plans','Follow bell/class schedule','Take attendance','Run lessons and activities','Leave a clear end-of-day handoff'];return <div className="space-y-6"><section className="rounded-[30px] bg-slate-950 p-8 text-white"><div className="text-xs font-black uppercase tracking-[.2em] text-sky-300">CaseCue Substitute</div><h1 className="mt-3 text-3xl font-black">Walk in. Know the day. Hand it back clean.</h1><p className="mt-2 text-slate-300">One simple substitute flow for a regular classroom or an authorized SPED assignment.</p></section><div className="grid gap-3 sm:grid-cols-2"><button onClick={()=>setType('general')} className={"rounded-2xl border p-5 text-left "+(type==='general'?'border-blue-500 bg-blue-50':'bg-white')}><b>General Substitute</b><p className="mt-1 text-sm text-slate-500">Schedule, lessons, attendance, activities and handoff.</p></button><button onClick={()=>setType('sped')} className={"rounded-2xl border p-5 text-left "+(type==='sped'?'border-blue-500 bg-blue-50':'bg-white')}><b>SPED Substitute</b><p className="mt-1 text-sm text-slate-500">Only approved supports needed for the assignment.</p></button></div><section className="rounded-2xl border bg-white p-6"><h2 className="font-black">Today’s Core Flow</h2><div className="mt-4 space-y-2">{flow.map((x,i)=><button key={x} onClick={()=>setDone(d=>d.includes(i)?d.filter(v=>v!==i):[...d,i])} className="flex w-full items-center gap-3 rounded-xl border p-4 text-left"><CheckCircle2 className={"h-5 w-5 "+(done.includes(i)?'text-emerald-600':'text-slate-300')}/><span className={done.includes(i)?'line-through text-slate-400':'font-bold'}>{i+1}. {x}</span></button>)}</div></section>{type==='sped'&&<div className="rounded-2xl border border-amber-200 bg-amber-50 p-5 text-sm text-amber-950"><ShieldCheck className="mr-2 inline h-4 w-4"/><b>SPED substitute mode is minimum-necessary.</b> It is not an IEP authoring workspace.</div>}</div>}
