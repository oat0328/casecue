import React,{useState}from'react';import{useNavigate}from'react-router-dom';import{ArrowRight,Check,Sparkles}from'lucide-react';import{base44}from'@/api/base44Client';import{useAuth}from'@/lib/AuthContext';import{WORKSPACES,workspaceHome,getUserWorkspaces}from'@/lib/workspaces';import{Button}from'@/components/ui/button';import{cn}from'@/lib/utils';
export default function WorkspaceSetup(){const{user,checkUserAuth}=useAuth(),navigate=useNavigate();const[selected,setSelected]=useState('sped'),[saving,setSaving]=useState(false);
const save=async()=>{setSaving(true);try{const existing=getUserWorkspaces(user);const workspaces=Array.from(new Set([...existing,selected]));await base44.auth.updateMe({workspaces,active_workspace:selected});await checkUserAuth();navigate(workspaceHome(selected),{replace:true});}finally{setSaving(false)}};
return <div className="min-h-screen bg-slate-950 px-4 py-12 text-white"><div className="mx-auto max-w-5xl"><div className="mb-8 flex items-center gap-3"><div className="flex h-11 w-11 items-center justify-center rounded-2xl bg-blue-500"><Sparkles className="h-5 w-5"/></div><div><div className="font-black">CaseCue</div><div className="text-xs text-slate-400">Choose your professional workspace</div></div></div><h1 className="text-3xl font-black tracking-tight">What do you do?</h1><p className="mt-2 max-w-2xl text-slate-400">CaseCue uses one account and one secure platform. Your role determines the workspace you see after login. You can add additional workspaces later.</p><div className="mt-8 grid gap-3 sm:grid-cols-2 lg:grid-cols-3">{Object.values(WORKSPACES).map(w=><button key={w.key} disabled={w.beta} onClick={()=>!w.beta&&setSelected(w.key)} className={cn("relative rounded-2xl border p-5 text-left transition",w.beta?"cursor-not-allowed border-white/10 bg-white/[.02] opacity-55":selected===w.key?"border-blue-400 bg-blue-500/10":"border-white/10 bg-white/[.04] hover:bg-white/[.07]")}><div className="flex items-center gap-2"><div className="font-black">{w.name}</div>{w.beta&&<span className="rounded-full border border-white/10 px-2 py-0.5 text-[10px] font-black uppercase tracking-wider text-sky-300">Private beta</span>}</div><div className="mt-2 text-sm leading-5 text-slate-400">{w.description}</div>{selected===w.key&&!w.beta&&<span className="absolute right-4 top-4 flex h-6 w-6 items-center justify-center rounded-full bg-blue-500"><Check className="h-4 w-4"/></span>}</button>)}</div><Button onClick={save} disabled={saving} className="mt-8 h-12 bg-blue-500 px-6 hover:bg-blue-600">{saving?'Setting up…':<>Enter {WORKSPACES[selected].name}<ArrowRight className="ml-2 h-4 w-4"/></>}</Button></div></div>}