import React,{useState}from"react";import{useNavigate}from"react-router-dom";import{Eye,Printer,FileDown,FileText,Copy,Star,BadgeCheck,Pencil,Trash2,PlayCircle}from"lucide-react";import{base44}from"@/api/base44Client";import{useAsync}from"@/lib/useAsync";import{useAuth}from"@/lib/AuthContext";import{useToast}from"@/components/ui/use-toast";import{Card}from"@/components/ui/cards";import{Button}from"@/components/ui/button";import{Dialog,DialogContent,DialogHeader,DialogTitle}from"@/components/ui/dialog";import{AlertDialog,AlertDialogContent,AlertDialogHeader,AlertDialogTitle,AlertDialogDescription,AlertDialogFooter,AlertDialogCancel,AlertDialogAction}from"@/components/ui/alert-dialog";import StudentSelector from"@/components/forms/StudentSelector";import{sortStudentIdsByName}from"@/lib/studentSort";import{exportLessonPdf,exportLessonDocx,printLesson,buildLessonHtml}from"@/lib/lessonExport";import{formatDate}from"@/lib/dateUtils";const inputCls="w-full h-11 rounded-lg border border-input bg-background px-3 text-base",labelCls="block text-sm font-medium mb-2";
export default function LessonLibrary({students,goals,onEdit,refreshKey}){const{user}=useAuth(),{toast}=useToast(),navigate=useNavigate();const[studentId,setStudentId]=useState(""),[subject,setSubject]=useState(""),[status,setStatus]=useState(""),[viewing,setViewing]=useState(null),[deleting,setDeleting]=useState(null),[deleteBusy,setDeleteBusy]=useState(false);const{data:lessons,refetch}=useAsync(()=>base44.entities.Lesson.list('-updated_date',200),[refreshKey]);const filtered=(lessons||[]).filter(l=>(!studentId||(l.student_ids||[]).includes(studentId))&&(!subject||(l.subject||"").toLowerCase().includes(subject.toLowerCase()))&&(!status||l.status===status));const audit=(l,action)=>[...(l.audit_trail||[]),{action,user_name:user?.full_name||user?.email||"",timestamp:new Date().toISOString()}];const update=async(l,patch,message)=>{try{await base44.entities.Lesson.update(l.id,patch);refetch();if(message)toast({title:message})}catch(e){toast({title:"Update failed",description:e.message,variant:"destructive"})}};const duplicate=async l=>{try{const{id,created_date,updated_date,created_by_id,...rest}=l;await base44.entities.Lesson.create({...rest,title:`${l.title} (copy)`,status:"draft",is_template:false,audit_trail:audit(l,"duplicated")});refetch();toast({title:"Lesson duplicated"})}catch(e){toast({title:"Duplicate failed",description:e.message,variant:"destructive"})}};const remove=async()=>{if(!deleting)return;setDeleteBusy(true);try{await base44.entities.Lesson.delete(deleting.id);setDeleting(null);refetch();toast({title:"Lesson plan deleted",description:"The lesson was removed from your Lesson Library."})}catch(e){toast({title:"Delete failed",description:e.message,variant:"destructive"})}finally{setDeleteBusy(false)}};const startSession=l=>{const p=new URLSearchParams(),sid=(l.student_ids||[])[0],gid=(l.goal_ids||[])[0];if(sid)p.set("student_id",sid);if(gid)p.set("goal_id",gid);if(l.plan?.subject_skill)p.set("activity",l.plan.subject_skill);navigate(`/session-tracker?${p}`)};const exportWithAudit=(kind,l)=>{try{kind==="pdf"?exportLessonPdf(l,students,goals):exportLessonDocx(l,students,goals);update(l,{audit_trail:audit(l,`exported_${kind}`)});toast({title:`Lesson plan downloaded (${kind.toUpperCase()})`})}catch(e){toast({title:"Export failed",description:e.message,variant:"destructive"})}};
return <div className="space-y-5"><Card className="p-5"><div className="grid sm:grid-cols-3 gap-5"><StudentSelector students={students||[]} value={studentId} onChange={setStudentId} noBottomSpace/><div><label className={labelCls}>Subject</label><input className={inputCls} value={subject} onChange={e=>setSubject(e.target.value)} placeholder="ELA, math…"/></div><div><label className={labelCls}>Status</label><select className={inputCls} value={status} onChange={e=>setStatus(e.target.value)}><option value="">All</option><option value="draft">Draft</option><option value="approved">Approved</option></select></div></div></Card>{filtered.length===0&&<Card className="p-8 text-center text-sm text-muted-foreground">No lessons found yet.</Card>}{filtered.map(l=><Card key={l.id} className="p-5"><div className="flex flex-col lg:flex-row lg:items-center justify-between gap-3"><div className="min-w-0"><div className="flex items-center gap-2 flex-wrap"><p className="font-semibold truncate">{l.title}</p>{l.is_template&&<span className="text-xs rounded-full bg-secondary px-2.5 py-0.5">Reusable Template</span>}<span className={`text-xs rounded-full px-2.5 py-0.5 ${l.status==="approved"?"bg-emerald-50 text-emerald-700":"bg-amber-50 text-amber-800"}`}>{l.status==="approved"?"Approved":"Draft"}</span></div><p className="text-xs text-muted-foreground mt-1">{l.subject||"—"} · {l.grade||"—"} · {formatDate(l.date)} · {sortStudentIdsByName(l.student_ids||[],students||[]).map(id=>{const s=(students||[]).find(x=>x.id===id);return s?`${s.first_name} ${s.last_name}`:null}).filter(Boolean).join(", ")||l.group_label||"no students attached"} · {(l.goal_ids||[]).length} goal{(l.goal_ids||[]).length===1?"":"s"}</p></div><div className="flex flex-wrap gap-2"><Button size="sm" variant="outline" onClick={()=>setViewing(l)}><Eye className="h-3.5 w-3.5 mr-1"/>View</Button><Button size="sm" variant="outline" onClick={()=>startSession(l)}><PlayCircle className="h-3.5 w-3.5 mr-1"/>Start session</Button><Button size="sm" variant="outline" onClick={()=>exportWithAudit("pdf",l)}><FileDown className="h-3.5 w-3.5 mr-1"/>PDF</Button><Button size="sm" variant="outline" onClick={()=>exportWithAudit("docx",l)}><FileText className="h-3.5 w-3.5 mr-1"/>DOCX</Button><Button size="sm" variant="outline" onClick={()=>{printLesson(l,students,goals);update(l,{audit_trail:audit(l,"printed")})}}><Printer className="h-3.5 w-3.5"/></Button>{l.status!=="approved"&&<Button size="sm" onClick={()=>update(l,{status:"approved",audit_trail:audit(l,"approved")},"Lesson approved")}><BadgeCheck className="h-3.5 w-3.5 mr-1"/>Approve</Button>}<Button size="sm" variant="outline" onClick={()=>duplicate(l)}><Copy className="h-3.5 w-3.5"/></Button><Button size="sm" variant="outline" onClick={()=>update(l,{is_template:!l.is_template,audit_trail:audit(l,l.is_template?"template removed":"saved as template")})}><Star className="h-3.5 w-3.5"/></Button><Button size="sm" variant="outline" onClick={()=>onEdit(l)}><Pencil className="h-3.5 w-3.5"/></Button><Button size="sm" variant="outline" title="Delete lesson plan" onClick={()=>setDeleting(l)}><Trash2 className="h-3.5 w-3.5 text-rose-500"/></Button></div></div></Card>)}<Dialog open={!!viewing} onOpenChange={o=>!o&&setViewing(null)}><DialogContent className="max-w-3xl"><DialogHeader><DialogTitle>{viewing?.title}</DialogTitle></DialogHeader>{viewing&&<><iframe title="Lesson plan preview" srcDoc={buildLessonHtml(viewing,students,goals)} className="w-full h-[60vh] rounded-lg border"/><div className="flex gap-2"><Button size="sm" onClick={()=>printLesson(viewing,students,goals)}><Printer className="h-3.5 w-3.5 mr-1"/>Print</Button><Button size="sm" variant="outline" onClick={()=>exportWithAudit("pdf",viewing)}>PDF</Button><Button size="sm" variant="outline" onClick={()=>exportWithAudit("docx",viewing)}>DOCX</Button></div></>}</DialogContent></Dialog><AlertDialog open={!!deleting} onOpenChange={o=>!o&&setDeleting(null)}><AlertDialogContent><AlertDialogHeader><AlertDialogTitle>Delete this lesson plan?</AlertDialogTitle><AlertDialogDescription>{deleting?`“${deleting.title}” will be permanently removed from your Lesson Library. This cannot be undone.`:''}</AlertDialogDescription></AlertDialogHeader><AlertDialogFooter><AlertDialogCancel disabled={deleteBusy}>Keep Lesson</AlertDialogCancel><AlertDialogAction disabled={deleteBusy} onClick={e=>{e.preventDefault();remove()}} className="bg-rose-600 hover:bg-rose-700">{deleteBusy?'Deleting…':'Delete Lesson'}</AlertDialogAction></AlertDialogFooter></AlertDialogContent></AlertDialog></div>}