import React, { useMemo } from "react";
import { useNavigate } from "react-router-dom";
import { AlertTriangle } from "lucide-react";
import { Card } from "@/components/ui/cards";
import { studentName, scheduledMinutes, deliveredThisWeek, findConflicts, DELIVERY_LABEL } from "@/lib/scheduleUtils";
export default function MinutesPanel({students,entries,sessionLogs}){const navigate=useNavigate();const scheduled=useMemo(()=>scheduledMinutes(entries),[entries]);const delivered=useMemo(()=>deliveredThisWeek(sessionLogs),[sessionLogs]);const conflicts=useMemo(()=>findConflicts(entries),[entries]);const conflictCount=useMemo(()=>{const out={};for(const c of conflicts)for(const id of c.student_ids)out[id]=(out[id]||0)+1;return out},[conflicts]);const roster=(students||[]).filter(s=>s.status!=="exited"&&s.roster_status!=="archived");const rows=roster.map(s=>{const required=Number.isFinite(Number(s.service_minutes))&&String(s.service_minutes??'')!==''?Number(s.service_minutes):null;const sched=scheduled[s.id]||0,deliv=delivered[s.id]||0;const missing=required!=null?Math.max(0,required-deliv):null,schedulingGap=required!=null?Math.max(0,required-sched):null;const status=required==null?"Needs IEP Minutes":sched<required?"Under Scheduled":deliv>=required?"Delivered":"In Progress";return{s,required,sched,deliv,missing,schedulingGap,status,conflicts:conflictCount[s.id]||0}});const styles={Delivered:"bg-emerald-100 text-emerald-700","In Progress":"bg-blue-100 text-blue-700","Under Scheduled":"bg-amber-100 text-amber-700","Needs IEP Minutes":"bg-rose-100 text-rose-700"};return <div className="space-y-4"><Card className="p-4 border-blue-100 bg-blue-50/40"><div className="font-semibold text-sm">How these numbers work</div><div className="text-xs text-slate-600 mt-1">Required / wk comes from the student's IEP service minutes saved in CaseCue. Scheduled / wk comes from active Instruction & Schedule groups. Delivered this wk comes from Session Tracker logs dated this week. Missing is Required minus Delivered. CaseCue will not guess required IEP minutes.</div></Card><Card className="p-0 overflow-hidden"><div className="overflow-x-auto"><table className="w-full text-sm"><thead><tr className="border-b bg-muted/50 text-left">{['Student','Required / wk','Scheduled / wk','Delivered this wk','Missing','Conflicts','Status'].map(x=><th key={x} className="px-4 py-3 font-semibold">{x}</th>)}</tr></thead><tbody>{rows.map(({s,required,sched,deliv,missing,schedulingGap,status,conflicts:c})=><tr key={s.id} className="border-b last:border-0"><td className="px-4 py-3 font-medium">{s.first_name} {s.last_name}{s.grade?` (${s.grade})`:''}</td><td className="px-4 py-3">{required!=null?`${required} min`:<button className="font-semibold text-blue-600 hover:underline" onClick={()=>navigate(`/students/${s.id}`)}>Add IEP minutes</button>}</td><td className={`px-4 py-3 ${schedulingGap>0?'text-amber-700 font-semibold':''}`}>{sched} min{schedulingGap>0&&<div className="text-[10px] font-normal">{schedulingGap} min under plan</div>}</td><td className="px-4 py-3">{deliv} min</td><td className={`px-4 py-3 ${missing>0?'text-amber-600 font-semibold':''}`}>{missing!=null?`${missing} min`:'—'}</td><td className={`px-4 py-3 ${c>0?'text-rose-600 font-semibold':''}`}>{c||'—'}</td><td className="px-4 py-3"><span className={`rounded-full px-2.5 py-1 text-xs font-semibold ${styles[status]}`}>{status}</span></td></tr>)}</tbody></table></div></Card>{conflicts.length>0&&<Card className="p-4"><h3 className="font-semibold text-sm flex items-center gap-1.5 text-rose-600"><AlertTriangle className="h-4 w-4"/>Scheduling Conflicts ({conflicts.length})</h3><ul className="mt-2 space-y-1.5 text-sm">{conflicts.map((c,i)=><li key={i}><span className="font-medium">{c.a.day}</span>: {c.a.group_name} ({c.a.start_time}–{c.a.end_time}, {DELIVERY_LABEL[c.a.delivery]||c.a.delivery}) overlaps {c.b.group_name} ({c.b.start_time}–{c.b.end_time}) for {c.student_ids.map(id=>studentName(students,id)).join(', ')}</li>)}</ul></Card>}</div>}
