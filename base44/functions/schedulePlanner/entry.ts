import { createClientFromRequest } from 'npm:@base44/sdk@0.8.44';

const SCHEMA={type:'object',properties:{summary:{type:'string'},groups:{type:'array',items:{type:'object',properties:{group_name:{type:'string'},delivery:{type:'string',enum:['pull-out','push-in','consultation']},day:{type:'string'},start_time:{type:'string'},end_time:{type:'string'},service_minutes:{type:'number'},teacher_classroom:{type:'string'},notes:{type:'string'},period:{type:'string'},recurrence_note:{type:'string'},week_pattern:{type:'string'},cycle_day:{type:'string'},goal_focus:{type:'array',items:{type:'string'}},students:{type:'array',items:{type:'object',properties:{name:{type:'string'},student_id:{type:'string'},match_type:{type:'string'},confidence:{type:'string'}},required:['name','student_id','match_type','confidence']}}},required:['group_name','delivery','day','start_time','end_time','students']}},conflicts:{type:'array',items:{type:'object',properties:{description:{type:'string'},severity:{type:'string'}},required:['description']}},unmatched_names:{type:'array',items:{type:'string'}},non_instructional_blocks:{type:'array',items:{type:'object',properties:{label:{type:'string'},day:{type:'string'},start_time:{type:'string'},end_time:{type:'string'},notes:{type:'string'}},required:['label','day','start_time','end_time']}},extraction_notes:{type:'array',items:{type:'string'}}},required:['groups','conflicts','extraction_notes']};

Deno.serve(async(req)=>{try{const base44=createClientFromRequest(req);const user=await base44.auth.me();if(!user)return Response.json({error:'Unauthorized'},{status:401});const body=await req.json();const uris=(body.file_uris||[]).filter(Boolean);if(!uris.length)return Response.json({error:'Upload at least one schedule file.'},{status:400});const urls=[];for(const uri of uris.slice(0,20)){const s=await base44.asServiceRole.integrations.Core.CreateFileSignedUrl({file_uri:String(uri),expires_in:600});if(s?.signed_url)urls.push(s.signed_url)}const students=(await base44.entities.Student.list('-updated_date',300)).filter(s=>s.roster_status!=='archived'&&s.status!=='exited');const goals=await base44.entities.Goal.list('-created_date',1000);const goalsBy={};for(const g of goals||[]){(goalsBy[g.student_id]??=[]).push(g.goal_area||g.description||'IEP goal')}
const roster=students.map(s=>`- ${s.first_name} ${s.last_name} | id:${s.id} | grade:${s.grade||'?'} | weekly minutes:${s.service_minutes??'unknown'} | goal areas:${(goalsBy[s.id]||[]).join(', ')||'none recorded'} | services:${(s.services||[]).join(', ')||'none recorded'}`).join('\n');
const rules=`Pull timing rule: ${body.pull_rule||'teacher choice'}; session length: ${body.session_minutes||30} minutes; max group size: ${body.max_group_size||4}; avoid: ${body.avoid||'none specified'}; preferred days: ${body.days||'Monday-Friday'}; delivery: ${body.delivery||'pull-out or push-in'}; additional request: ${body.pull_preferences||'none'}.`;
const prompt=`You are CaseCue's special education scheduling planner. Analyze ALL uploaded files. They may include a SCHOOL BELL SCHEDULE, student schedules, a SPED/resource schedule, Excel/CSV tables, PDFs, Word exports, Google Docs/Sheets exports, or screenshots.\n\nACTIVE CASELOAD WITH ACTUAL IEP GOAL AREAS:\n${roster}\n\nTEACHER RULES:\n${rules}\n\nBuild an advisory proposed service schedule. The educator must approve it.\n\nRules:\n1. Separate bell schedule facts from student class schedules and SPED service blocks. Never treat a Gen Ed class as an already-delivered SPED service.\n2. Group students primarily by compatible IEP goal areas/instructional needs, then schedule availability. Do not group only because students share a grade.\n3. If pull_rule says last N minutes, derive the window from each class/bell period end time exactly. If it says first N minutes, derive from period start. If the necessary bell/class time is not visible, flag it instead of guessing.\n4. Respect max group size and avoid rules such as PE, lunch, electives, testing, or teacher planning when those are visible in uploaded schedules.\n5. Use only active roster students. Match names conservatively. Unknown names remain unmatched.\n6. Expand recurring sessions across actual weekdays.\n7. Compare each student's proposed weekly scheduled minutes with CaseCue service_minutes and create an alert for under/over scheduling. This is a planning alert, not a legal determination.\n8. Create alerts for overlaps, students with no group, groups above preferred size, missing bell schedule information, missing student schedules, and students whose goals do not align with their proposed group.\n9. Non-instructional teacher blocks (lunch, prep, duty, meetings) go in non_instructional_blocks.\n10. In each group's notes explain WHY those students belong together using their goal areas and any scheduling constraint. Keep it concise.\n11. Return one group entry per weekday/time block. Times are 24-hour HH:MM.\n12. Never invent IEP goals, service minutes, class periods, or student schedules.\n\nReturn JSON exactly matching the schema.`;
const result=await base44.asServiceRole.integrations.Core.InvokeLLM({prompt,file_urls:urls,response_json_schema:SCHEMA});return Response.json(result);}catch(e){return Response.json({error:e?.message||'Schedule planner failed'},{status:500})}});
